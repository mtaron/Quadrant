namespace Quadrant.Telemetry
{
    public static class TelemetryMetrics
    {
        public const string Load = nameof(Load);
        public const string DrawGraph = nameof(DrawGraph);
        public const string DrawFunction = nameof(DrawFunction);
    }
}
